export const MAJOR_VERSION = 1;
export const MINOR_VERSION = 0;
export const PATCH_VERSION = 0;

export const API_VERSION = `${MAJOR_VERSION}.${MINOR_VERSION}.${PATCH_VERSION}`;

export const TRACE_ID_LENGTH = 16;
export const TRACE_ID_PREFIX = "trace";
